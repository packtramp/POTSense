{
  "buildCommand": "npx expo export --platform web",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/((?!_expo|api|icon-|manifest|favicon|sw).*)", "destination": "/" }
  ]
}
